// Define the array of favorite modes of transportation
const favoriteTransportation: string[] = ["supra mk4", "Mustang", "Grande", "civic"];

// Loop through the array and print statements about each item
favoriteTransportation.forEach((transport: string) => {
    console.log(`I would like to own a ${transport}.`);
});
