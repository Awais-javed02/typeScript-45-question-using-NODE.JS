// define a function to print each magicain name from an array
function show_magicains(magicains:string[]){
    magicains.forEach  (name => console.log(name))
}

//define an array containing array name
let magicain_name = ["Harry porter","Awais","Anus"];

// Call the the function to print each magicains name
show_magicains(magicain_name);